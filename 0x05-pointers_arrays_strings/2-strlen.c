#include "main.h"

/**
 * _srtlen - calculate the length of a string.
 *
 * @s: the string calculate it's length.
 * Return: Always (0)
 */

int_srtlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}

	return (i);
}
